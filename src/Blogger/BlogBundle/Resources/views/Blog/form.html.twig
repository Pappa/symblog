{# src/Blogger/BlogBundle/Resources/views/Blog/form.html.twig #}

<form action="{{ path('BloggerBlogBundle_blog_edit', { 'id': blog.id, 'slug': blog.slug }) }}" method="post" {{ form_enctype(form) }} class="blogger">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>